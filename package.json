{
  "name": "projeyucely",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 8787",
    "deploy": "wrangler deploy",
    "test": "npm run test:worker",
    "test:worker": "node --test cloudflare/worker/test/*.test.js",
    "check:worker": "for file in cloudflare/worker/src/*.js cloudflare/worker/test/*.js cloudflare/worker/worker.test.js; do node --check \"$file\" || exit 1; done",
    "build:worker": "wrangler deploy --dry-run",
    "audit:dependencies": "npm audit --audit-level=high",
    "ci": "npm run check:worker && npm run test:worker && npm run build:worker && npm run audit:dependencies"
  },
  "devDependencies": {
    "wrangler": "^4.120.1"
  }
}
